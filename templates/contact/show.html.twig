{% extends 'base.html.twig' %}

    {% block body %}

        <main class="container">
            <ul>
                <li>Prénom : {{contact.firstname}}</li>
                <li>Nom : {{contact.lastname}}</li>
                <li>Tel : {{contact.phoneNumber}}</li>
                <li>Email : {{contact.email}}</li>
            </ul>
        </main>
        
    {% endblock %}